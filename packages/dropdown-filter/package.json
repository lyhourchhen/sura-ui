{
  "name": "@asurraa/sura-ui-dropdown-filter",
  "version": "1.0.18",
  "description": "Utilities function use internally at AsurRaa.",
  "author": "@asurraa",
  "main": "dist/index.js",
  "source": "src/index.tsx",
  "scripts": {
    "prebuild": "rimraf dist",
    "dev": "tsc -w",
    "build": "tsc --build tsconfig.json",
    "deps": "yarn upgrade-interactive --latest",
    "prepublish": "yarn build",
    "prettier": "prettier -c --write \"**/*\""
  },
  "dependencies": {
    "@asurraa/sura-ui-select": "^1.0.5"
  },
  "files": [
    "dist"
  ]
}
